/**
 * @type {import('prettier').Config}
 */
module.exports = { ...require('prettier-config-polished') };

// ? 08.05.2023: coudn't make single .prettierrc to cover entire monorepo, so each package has its own .prettierrc
