{
    "$schema": "https://turbo.build/schema.json",
    "extends": ["//"],
    "tasks": {
        "seed": {
            "dependsOn": ["prisma:migrate:reset"],
            "cache": false
        },
        "prisma:migrate:reset": {
            "cache": false
        }
    }
}
