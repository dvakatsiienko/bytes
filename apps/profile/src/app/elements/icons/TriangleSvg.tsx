'use client';

/* Core */
import { Triangle } from '@styled-icons/ionicons-sharp/Triangle';
import type { StyledIconProps } from '@styled-icons/styled-icon';

export const TriangleSvg = (props: StyledIconProps) => {
    return <Triangle { ...props } />;
};
