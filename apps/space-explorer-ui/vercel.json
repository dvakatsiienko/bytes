{
    "$schema": "https://openapi.vercel.sh/vercel.json",
    "buildCommand": "turbo build --filter='@space-explorer/ui' --color",
    "installCommand": "pnpm install",
    "rewrites": [{ "source": "/(.*)", "destination": "/" }]
}
