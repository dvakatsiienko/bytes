{
    "$schema": "https://turbo.build/schema.json",
    "tasks": {
        "start": {
            "cache": false,
            "persistent": true
        },
        "build": {
            "dependsOn": ["^build", "lint", "typecheck"],
            "outputs": [".next/**", "!.next/cache/**"]
        },
        "lint": {},
        "typecheck": {
            "dependsOn": ["^typecheck"]
        }
    }
}
