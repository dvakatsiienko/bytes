{
  "$schema": "https://v2-10-0.turborepo.dev/schema.json",
  "ui": "tui",
  // Declaring this list makes turbo strip every OTHER env var from task
  // environments, so anything the tasks genuinely need must be named here.
  "globalPassThroughEnv": ["ENABLE_EXPERIMENTAL_COREPACK", "DATABASE_URL"],
  "tasks": {
    "dev": {
      "cache": false,
      "persistent": true,
      "dependsOn": ["^prisma:generate"]
    },
    "serve": { "cache": false, "persistent": true },
    "dev:convex": { "cache": false, "persistent": true },
    "build": {
      "dependsOn": ["^build", "lint", "typecheck", "^prisma:generate"],
      "outputs": [".next/**", "!.next/cache/**"],
      "inputs": [
        "$TURBO_DEFAULT$",

        "!.vscode/**",
        "!.cursor/**",
        "!.editorconfig",

        "!CLAUDE.md",
        "!**/**/README.md",

        "!config/biome.*.jsonc",
        "!config/biome.jsonc",
        "!prettier.config.mjs",
        "!lefthook.yaml",
        "!coderabbit.yaml"
      ]
    },
    "lint": {},
    "typecheck": {
      "dependsOn": ["^typecheck", "prisma:generate"],
      "description": "Checks TypeScript code for errors",
      "inputs": [
        "$TURBO_DEFAULT$",
        "package.json",
        "tsconfig.json",

        "**/*.{ts,tsx}",
        "**/*.d.ts"
      ]
    },

    "prisma:generate": {
      "inputs": ["./prisma/**/*"]
    }
  }
}
